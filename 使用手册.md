# 同学蹭饭图 使用手册

## 一、快速开始

### 启动网站
双击 `启动蹭饭图.bat`，浏览器会自动打开网站。

### 首页概览

| 区域 | 内容 |
|------|------|
| 左侧幕布 | 同学们最新发布的照片，自动滚动，鼠标悬停暂停 |
| 中间 | 中国地图，颜色越深表示该省同学越多，橙色圆圈显示人数 |
| 右侧幕布 | 同学们最新留言，自动滚动 |
| 下方信息栏 | 实时时钟、当地天气、同学统计 |

---

## 二、浏览地图

1. **查看全国**：首页显示中国地图，鼠标悬停省份会高亮显示
2. **进入省份**：点击任意省份，进入该省详情页，显示省内各城市地图
3. **查看同学**：省份页右侧显示同学列表，按城市分组
4. **同学信息卡**：鼠标悬停同学姓名，弹出联系方式（手机、微信）
5. **个人主页**：点击同学姓名，进入详细个人页面

### 个人主页包含

- 基本信息（大学、专业）
- 联系方式（手机、微信、QQ）
- 学业信息（入学年份、高中班级、所在城市）
- 个性留言
- 留言板（可发留言、上传照片）

---

## 三、注册与登录

### 注册步骤

1. 点击右上角 **"注册"** 按钮
2. 填写邮箱和密码（密码至少6位），点击"下一步"
3. 填写同学信息：姓名、大学、专业、省份、城市等
4. 提交后状态为 **"待审核"**，需管理员通过后才会在地图上显示
5. 邮箱会收到验证邮件，请及时验证

### 登录

- 点击 **"登录"** 按钮，输入邮箱和密码
- 登录后右上角显示邮箱地址和"退出"按钮

---

## 四、留言板

### 发表留言

1. 进入任意同学的个人主页
2. 在留言板区域点击 **"写留言"**
3. 输入文字内容，可点击 **"+ 上传照片"** 添加图片
4. 点击 **"发送"** 即可

### 删除留言

- 每条留言右上角有"删除"按钮
- 只能删除自己发表的留言

---

## 五、管理员功能

### 首次设置

1. 在 [Supabase](https://supabase.com) 创建项目
2. 在 SQL 编辑器中依次运行 `supabase/migrations/` 下的 SQL 文件
3. 在 Supabase 控制台 → Authentication → Users 中找到管理员的用户 ID
4. 在 `admins` 表中手动插入该用户 ID
5. 创建 `.env` 文件：

```
VITE_SUPABASE_URL=https://你的项目.supabase.co
VITE_SUPABASE_ANON_KEY=你的anon-key
```

### 管理后台

登录管理员账号后，右上角出现 **"管理后台"** 入口，可以：

- **审核同学**：查看待审核列表，通过或拒绝
- **编辑信息**：修改任意同学的信息
- **删除记录**：删除不实或过期的同学信息
- **撤回通过**：将已通过的同学改回待审核状态
- **添加管理员**：在 admins 表中添加新管理员的用户 ID

---

## 六、部署到线上

### 前提
- 已配置好 Supabase 数据库
- `.env` 文件已填写正确的 Supabase 信息

### 步骤

1. 安装 Vercel CLI：`npm i -g vercel`
2. 在项目目录执行：`vercel`
3. 按提示完成部署
4. 在 Vercel 项目设置中添加环境变量：
   - `VITE_SUPABASE_URL`
   - `VITE_SUPABASE_ANON_KEY`
5. 重新部署使环境变量生效

部署完成后，把网址分享给同学即可访问。

---

## 七、常见问题

**Q：开发模式下数据存在哪里？**
A：存在浏览器的 localStorage 中，清除浏览器缓存会丢失数据。配置 Supabase 后数据存在云端。

**Q：地图上的省份颜色代表什么？**
A：米色 = 没有同学，颜色越深 = 该省同学越多。

**Q：为什么我注册后地图上看不到自己？**
A：新注册需要管理员审核通过后才会在地图上显示。

**Q：留言中的照片有大小限制吗？**
A：建议单张照片不超过 5MB，支持 JPG、PNG 等常见格式。

**Q：天气显示的是哪个城市？**
A：默认显示武汉，登录后自动切换到你所在城市。
